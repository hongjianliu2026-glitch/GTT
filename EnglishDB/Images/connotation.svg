<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 800 600">
  <defs>
    <style>
      .background { fill: #F9EBEA; }
      .title { font: bold 48px sans-serif; fill: #C0392B; }
      .subtitle { font: italic 24px serif; fill: #2C3E50; }
      .content { font: 18px sans-serif; fill: #34495E; }
      .highlight { font-weight: bold; fill: #E74C3C; }
      .story { font: 16px serif; fill: #2980B9; }
      .image { stroke: #7F8C8D; stroke-width: 2; fill: none; }
    </style>
  </defs>

  <!-- Background -->
  <rect class="background" width="100%" height="100%" />

  <!-- Title -->
  <text x="50" y="80" class="title">connotation</text>
  <text x="50" y="120" class="subtitle">[ˌkɒnəˈteɪʃn] n. 内涵，隐含意义</text>

  <!-- Etymology -->
  <text x="50" y="170" class="content">
    <tspan class="highlight">词根:</tspan> con- (一起) + not (标记) + -ation (名词后缀)
  </text>

  <!-- Story -->
  <text x="50" y="210" class="story">
    想象一个神秘的符号，"一起标记(con-not)"
    <tspan x="50" dy="25">在文字中，这个符号隐含着深层的意义(connotation)。</tspan>
  </text>

  <!-- Example -->
  <text x="50" y="280" class="content">
    <tspan class="highlight">例句:</tspan> The word "home" has positive connotations for most people.
  </text>
  <text x="50" y="310" class="content">
    "家"这个词对大多数人来说有积极的内涵。
  </text>

  <!-- Visual representation -->
  <g transform="translate(400,320)">
    <!-- Symbol -->
    <circle cx="100" cy="100" r="50" class="image" />
    <line x1="100" y1="50" x2="100" y2="150" class="image" />
    <line x1="50" y1="100" x2="150" y2="100" class="image" />
    <!-- Text -->
    <text x="180" y="100" class="content" text-anchor="middle">隐含意义</text>
  </g>
</svg>