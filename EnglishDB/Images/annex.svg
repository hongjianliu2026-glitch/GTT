<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 800 600">
  <defs>
    <style>
      .background { fill: #E8F6F3; }
      .title { font: bold 48px sans-serif; fill: #16A085; }
      .subtitle { font: italic 24px serif; fill: #2C3E50; }
      .content { font: 18px sans-serif; fill: #34495E; }
      .highlight { font-weight: bold; fill: #E74C3C; }
      .story { font: 16px serif; fill: #2980B9; }
      .image { stroke: #7F8C8D; stroke-width: 2; fill: none; }
    </style>
  </defs>

  <!-- Background -->
  <rect class="background" width="100%" height="100%" />

  <!-- Title -->
  <text x="50" y="80" class="title">annex</text>
  <text x="50" y="120" class="subtitle">[əˈnɛks] v. 附加，吞并；n. 附件，附属建筑物</text>

  <!-- Etymology -->
  <text x="50" y="170" class="content">
    <tspan class="highlight">词根:</tspan> an- (向) + nex (连接)
  </text>

  <!-- Story -->
  <text x="50" y="210" class="story">
    想象一座大房子，"向(an-)"
    <tspan x="50" dy="25">连接(nex)"了一个小屋，这个小屋就是房子的附属建筑物(annex)。</tspan>
  </text>

  <!-- Example -->
  <text x="50" y="280" class="content">
    <tspan class="highlight">例句:</tspan> The library was expanded by annexing an old warehouse.
  </text>
  <text x="50" y="310" class="content">
    图书馆通过吞并一个旧仓库进行了扩建。
  </text>

  <!-- Visual representation -->
  <g transform="translate(400,320)">
    <!-- Main house -->
    <rect x="0" y="0" width="200" height="150" class="image" />
    <!-- Annex -->
    <rect x="220" y="50" width="100" height="100" class="image" />
    <!-- Connecting line -->
    <line x1="200" y1="125" x2="220" y2="100" class="image" />
  </g>
</svg>