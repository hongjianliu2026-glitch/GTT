<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 800 600">
  <defs>
    <style>
      .background { fill: #E8F6F3; }
      .title { font: bold 48px sans-serif; fill: #16A085; }
      .subtitle { font: italic 24px serif; fill: #2C3E50; }
      .content { font: 18px sans-serif; fill: #34495E; }
      .highlight { font-weight: bold; fill: #E74C3C; }
      .story { font: 16px serif; fill: #2980B9; }
      .image { stroke: #7F8C8D; stroke-width: 2; fill: none; }
    </style>
  </defs>

  <!-- Background -->
  <rect class="background" width="100%" height="100%" />

  <!-- Title -->
  <text x="50" y="80" class="title">quickest</text>
  <text x="50" y="120" class="subtitle">[ˈkwɪkɪst] adj. 最快的</text>

  <!-- Etymology -->
  <text x="50" y="170" class="content">
    <tspan class="highlight">词根:</tspan> quick (快) + -est (最高级)
  </text>

  <!-- Story -->
  <text x="50" y="210" class="story">
    想象一只猎豹在草原上飞奔，"快得(quick)"
    <tspan x="50" dy="25">像闪电一样，它就是最快的(quickest)。</tspan>
  </text>

  <!-- Example -->
  <text x="50" y="280" class="content">
    <tspan class="highlight">例句:</tspan> The quickest way to get there is by helicopter.
  </text>
  <text x="50" y="310" class="content">
    到达那里最快的方式是乘坐直升机。
  </text>

  <!-- Visual representation -->
  <g transform="translate(400,320)">
    <!-- Cheetah -->
    <path d="M0 100 L50 50 L100 100 L150 50 L200 100 L250 50 L300 100" class="image" />
    <!-- Lightning bolt -->
    <path d="M50 150 L100 100 L150 150 L200 100 L250 150" class="image" />
    <!-- Ground -->
    <line x1="0" y1="200" x2="350" y2="200" class="image" />
  </g>
</svg>