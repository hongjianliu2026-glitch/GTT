<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 800 600">
  <defs>
    <style>
      .background { fill: #E8F6F3; }
      .title { font: bold 48px sans-serif; fill: #16A085; }
      .subtitle { font: italic 24px serif; fill: #2C3E50; }
      .content { font: 18px sans-serif; fill: #34495E; }
      .highlight { font-weight: bold; fill: #E74C3C; }
      .story { font: 16px serif; fill: #2980B9; }
      .image { stroke: #7F8C8D; stroke-width: 2; fill: none; }
    </style>
  </defs>

  <!-- Background -->
  <rect class="background" width="100%" height="100%" />

  <!-- Title -->
  <text x="50" y="80" class="title">prevent</text>
  <text x="50" y="120" class="subtitle">[prɪˈvɛnt] v. 防止，阻止</text>

  <!-- Etymology -->
  <text x="50" y="170" class="content">
    <tspan class="highlight">词根:</tspan> pre- (在前面) + vent (来)
  </text>

  <!-- Story -->
  <text x="50" y="210" class="story">
    想象一个人站在前方，"提前(pre-)"
    <tspan x="50" dy="25">挡住(vent)了危险的到来，成功阻止(prevent)了事故。</tspan>
  </text>

  <!-- Example -->
  <text x="50" y="280" class="content">
    <tspan class="highlight">例句:</tspan> Wearing a helmet can prevent head injuries.
  </text>
  <text x="50" y="310" class="content">
    戴头盔可以防止头部受伤。
  </text>

  <!-- Visual representation -->
  <g transform="translate(400,320)">
    <!-- Person blocking -->
    <circle cx="50" cy="100" r="20" class="image" />
    <line x1="50" y1="120" x2="50" y2="180" class="image" />
    <line x1="50" y1="180" x2="30" y2="220" class="image" />
    <line x1="50" y1="180" x2="70" y2="220" class="image" />
    <line x1="50" y1="150" x2="90" y2="140" class="image" />
    <!-- Danger symbol -->
    <polygon points="150,100 180,160 120,160" class="image" />
    <line x1="150" y1="100" x2="150" y2="160" class="image" />
    <!-- Barrier -->
    <rect x="30" y="200" width="140" height="20" class="image" />
  </g>
</svg>