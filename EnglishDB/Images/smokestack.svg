<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 800 600">
  <defs>
    <style>
      .background { fill: #E8F6F3; }
      .title { font: bold 48px sans-serif; fill: #16A085; }
      .subtitle { font: italic 24px serif; fill: #2C3E50; }
      .content { font: 18px sans-serif; fill: #34495E; }
      .highlight { font-weight: bold; fill: #E74C3C; }
      .story { font: 16px serif; fill: #2980B9; }
      .image { stroke: #7F8C8D; stroke-width: 2; fill: none; }
      .smoke { fill: #BDC3C7; }
    </style>
  </defs>

  <!-- Background -->
  <rect class="background" width="100%" height="100%" />

  <!-- Title -->
  <text x="50" y="80" class="title">smokestack</text>
  <text x="50" y="120" class="subtitle">[ˈsmoʊkstæk] n. 烟囱，烟筒</text>

  <!-- Etymology -->
  <text x="50" y="170" class="content">
    <tspan class="highlight">词根:</tspan> smoke (烟) + stack (堆叠)
  </text>

  <!-- Story -->
  <text x="50" y="210" class="story">
    想象一个巨大的烟囱，"堆叠(stack)"着浓密的烟雾(smoke)，
    <tspan x="50" dy="25">这些烟雾从烟囱中不断冒出，形成了一个壮观的景象。</tspan>
  </text>

  <!-- Example -->
  <text x="50" y="280" class="content">
    <tspan class="highlight">例句:</tspan> The smokestack of the factory emitted thick smoke all day.
  </text>
  <text x="50" y="310" class="content">
    工厂的烟囱一整天都在冒出浓烟。
  </text>

  <!-- Visual representation -->
  <g transform="translate(400,320)">
    <!-- Smokestack -->
    <rect x="0" y="0" width="100" height="200" class="image" />
    <!-- Smoke -->
    <ellipse cx="50" cy="0" rx="50" ry="20" class="smoke" />
    <ellipse cx="50" cy="-30" rx="60" ry="25" class="smoke" />
    <ellipse cx="50" cy="-60" rx="70" ry="30" class="smoke" />
  </g>
</svg>