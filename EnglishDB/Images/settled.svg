<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 800 600">
  <defs>
    <style>
      .background { fill: #E8F6F3; }
      .title { font: bold 48px sans-serif; fill: #16A085; }
      .subtitle { font: italic 24px serif; fill: #2C3E50; }
      .content { font: 18px sans-serif; fill: #34495E; }
      .highlight { font-weight: bold; fill: #E74C3C; }
      .story { font: 16px serif; fill: #2980B9; }
      .image { stroke: #7F8C8D; stroke-width: 2; fill: none; }
    </style>
  </defs>

  <!-- Background -->
  <rect class="background" width="100%" height="100%" />

  <!-- Title -->
  <text x="50" y="80" class="title">settled</text>
  <text x="50" y="120" class="subtitle">[ˈsɛtld] adj. 稳定的，固定的</text>

  <!-- Etymology -->
  <text x="50" y="170" class="content">
    <tspan class="highlight">词根:</tspan> set (放置) + -ed (过去分词后缀)
  </text>

  <!-- Story -->
  <text x="50" y="210" class="story">
    想象一个石头被"放置(set)"在地面，
    <tspan x="50" dy="25">它不再移动，变得"稳定(settled)"。</tspan>
  </text>

  <!-- Example -->
  <text x="50" y="280" class="content">
    <tspan class="highlight">例句:</tspan> After years of moving, they finally settled in a small town.
  </text>
  <text x="50" y="310" class="content">
    经过多年的搬迁，他们终于在一个小镇定居下来。
  </text>

  <!-- Visual representation -->
  <g transform="translate(400,320)">
    <!-- Ground -->
    <rect x="0" y="150" width="350" height="50" class="image" />
    <!-- Stone -->
    <circle cx="175" cy="120" r="30" class="image" />
    <!-- Arrow -->
    <line x1="175" y1="120" x2="175" y2="150" class="image" />
    <polygon points="170,145 180,145 175,150" class="image" />
  </g>
</svg>