<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 800 600">
  <defs>
    <style>
      .background { fill: #E8F6F3; }
      .title { font: bold 48px sans-serif; fill: #16A085; }
      .subtitle { font: italic 24px serif; fill: #2C3E50; }
      .content { font: 18px sans-serif; fill: #34495E; }
      .highlight { font-weight: bold; fill: #E74C3C; }
      .story { font: 16px serif; fill: #2980B9; }
      .image { stroke: #7F8C8D; stroke-width: 2; fill: none; }
    </style>
  </defs>

  <!-- Background -->
  <rect class="background" width="100%" height="100%" />

  <!-- Title -->
  <text x="50" y="80" class="title">folding</text>
  <text x="50" y="120" class="subtitle">[ˈfoʊldɪŋ] n. 折叠，折痕；adj. 可折叠的</text>

  <!-- Etymology -->
  <text x="50" y="170" class="content">
    <tspan class="highlight">词根:</tspan> fold (折叠) + -ing (表示动作或状态)
  </text>

  <!-- Story -->
  <text x="50" y="210" class="story">
    想象一张巨大的纸，"折叠(fold)"成各种形状，
    <tspan x="50" dy="25">这些形状就像魔术一样变幻莫测，这就是折叠(folding)的魅力。</tspan>
  </text>

  <!-- Example -->
  <text x="50" y="280" class="content">
    <tspan class="highlight">例句:</tspan> The magician performed a trick with a folding fan.
  </text>
  <text x="50" y="310" class="content">
    魔术师用一把折叠扇表演了一个魔术。
  </text>

  <!-- Visual representation -->
  <g transform="translate(400,320)">
    <!-- Paper -->
    <rect x="0" y="0" width="350" height="200" class="image" />
    <!-- Fold lines -->
    <line x1="50" y1="50" x2="300" y2="50" class="image" />
    <line x1="50" y1="100" x2="300" y2="100" class="image" />
    <line x1="50" y1="150" x2="300" y2="150" class="image" />
    <!-- Folded paper -->
    <rect x="100" y="20" width="150" height="160" class="image" />
    <rect x="120" y="40" width="110" height="120" class="image" />
    <rect x="140" y="60" width="70" height="80" class="image" />
  </g>
</svg>