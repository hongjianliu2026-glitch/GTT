<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 800 600">
  <defs>
    <style>
      .background { fill: #E8F6F3; }
      .title { font: bold 48px sans-serif; fill: #16A085; }
      .subtitle { font: italic 24px serif; fill: #2C3E50; }
      .content { font: 18px sans-serif; fill: #34495E; }
      .highlight { font-weight: bold; fill: #E74C3C; }
      .story { font: 16px serif; fill: #2980B9; }
      .image { stroke: #7F8C8D; stroke-width: 2; fill: none; }
    </style>
  </defs>

  <!-- Background -->
  <rect class="background" width="100%" height="100%" />

  <!-- Title -->
  <text x="50" y="80" class="title">open</text>
  <text x="50" y="120" class="subtitle">[ˈoʊpən] adj. 开着的，开放的；v. 打开</text>

  <!-- Etymology -->
  <text x="50" y="170" class="content">
    <tspan class="highlight">词根:</tspan> op- (朝向) + en (使)
  </text>

  <!-- Story -->
  <text x="50" y="210" class="story">
    想象一扇门被"朝向(op-)"外"使(en)"打开，
    <tspan x="50" dy="25">展现出一片广阔的视野，这就是"open"的画面。</tspan>
  </text>

  <!-- Example -->
  <text x="50" y="280" class="content">
    <tspan class="highlight">例句:</tspan> She opened the door and stepped outside.
  </text>
  <text x="50" y="310" class="content">
    她打开门，走了出去。
  </text>

  <!-- Visual representation -->
  <g transform="translate(400,320)">
    <!-- Door frame -->
    <rect x="0" y="0" width="350" height="200" class="image" />
    <!-- Door -->
    <rect x="50" y="20" width="150" height="160" class="image" />
    <!-- Open door -->
    <rect x="200" y="20" width="150" height="160" class="image" transform="rotate(30,275,100)" />
    <!-- Horizon -->
    <line x1="0" y1="200" x2="350" y2="200" class="image" />
    <!-- Sun -->
    <circle cx="300" cy="150" r="30" class="image" />
  </g>
</svg>