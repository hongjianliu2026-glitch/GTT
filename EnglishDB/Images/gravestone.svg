<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 800 600">
  <defs>
    <style>
      .background { fill: #E8F6F3; }
      .title { font: bold 48px sans-serif; fill: #16A085; }
      .subtitle { font: italic 24px serif; fill: #2C3E50; }
      .content { font: 18px sans-serif; fill: #34495E; }
      .highlight { font-weight: bold; fill: #E74C3C; }
      .story { font: 16px serif; fill: #2980B9; }
      .image { stroke: #7F8C8D; stroke-width: 2; fill: none; }
    </style>
  </defs>

  <!-- Background -->
  <rect class="background" width="100%" height="100%" />

  <!-- Title -->
  <text x="50" y="80" class="title">gravestone</text>
  <text x="50" y="120" class="subtitle">[ˈɡreɪvstoʊn] n. 墓碑</text>

  <!-- Etymology -->
  <text x="50" y="170" class="content">
    <tspan class="highlight">词根:</tspan> grav (重) + stone (石头)
  </text>

  <!-- Story -->
  <text x="50" y="210" class="story">
    想象一块沉重的石头(grav-stone)，它被用来标记一个人的安息之地，
    <tspan x="50" dy="25">这块石头就是墓碑(gravestone)。</tspan>
  </text>

  <!-- Example -->
  <text x="50" y="280" class="content">
    <tspan class="highlight">例句:</tspan> The gravestone was engraved with the deceased's name.
  </text>
  <text x="50" y="310" class="content">
    墓碑上刻着逝者的名字。
  </text>

  <!-- Visual representation -->
  <g transform="translate(400,320)">
    <!-- Gravestone -->
    <rect x="0" y="0" width="350" height="200" class="image" />
    <!-- Stone texture -->
    <rect x="50" y="20" width="250" height="160" class="image" />
    <!-- Cross on the gravestone -->
    <line x1="175" y1="50" x2="175" y2="150" class="image" />
    <line x1="125" y1="100" x2="225" y2="100" class="image" />
    <!-- Name engraving -->
    <text x="130" y="130" class="content" fill="#FFFFFF">John Doe</text>
  </g>
</svg>