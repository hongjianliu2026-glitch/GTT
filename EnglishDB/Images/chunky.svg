<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 800 600">
  <defs>
    <style>
      .background { fill: #F9EBEA; }
      .title { font: bold 48px sans-serif; fill: #C0392B; }
      .subtitle { font: italic 24px serif; fill: #7B241C; }
      .content { font: 18px sans-serif; fill: #512E5F; }
      .highlight { font-weight: bold; fill: #E74C3C; }
      .story { font: 16px serif; fill: #8E44AD; }
      .image { stroke: #7F8C8D; stroke-width: 2; fill: none; }
    </style>
  </defs>

  <!-- Background -->
  <rect class="background" width="100%" height="100%" />

  <!-- Title -->
  <text x="50" y="80" class="title">chunky</text>
  <text x="50" y="120" class="subtitle">[ˈtʃʌŋki] adj. 厚实的，大块的</text>

  <!-- Etymology -->
  <text x="50" y="170" class="content">
    <tspan class="highlight">词根:</tspan> chunk (块) + -y (形容词后缀)
  </text>

  <!-- Story -->
  <text x="50" y="210" class="story">
    想象一个巨大的"块(chunk)"，它如此厚实，以至于你无法轻易移动它。
    <tspan x="50" dy="25">这个"块"就是"chunky"的象征，代表着厚实和大块的特征。</tspan>
  </text>

  <!-- Example -->
  <text x="50" y="280" class="content">
    <tspan class="highlight">例句:</tspan> The soup had chunky pieces of vegetables.
  </text>
  <text x="50" y="310" class="content">
    汤里有厚实的蔬菜块。
  </text>

  <!-- Visual representation -->
  <g transform="translate(400,320)">
    <!-- Chunky block -->
    <rect x="0" y="0" width="200" height="200" class="image" />
    <!-- Label -->
    <text x="100" y="220" class="content" text-anchor="middle">Chunky Block</text>
  </g>
</svg>