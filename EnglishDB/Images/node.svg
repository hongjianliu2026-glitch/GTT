<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 800 600">
  <defs>
    <style>
      .background { fill: #E8F6F3; }
      .title { font: bold 48px sans-serif; fill: #16A085; }
      .subtitle { font: italic 24px serif; fill: #2C3E50; }
      .content { font: 18px sans-serif; fill: #34495E; }
      .highlight { font-weight: bold; fill: #E74C3C; }
      .story { font: 16px serif; fill: #2980B9; }
      .image { stroke: #7F8C8D; stroke-width: 2; fill: none; }
    </style>
  </defs>

  <!-- Background -->
  <rect class="background" width="100%" height="100%" />

  <!-- Title -->
  <text x="50" y="80" class="title">node</text>
  <text x="50" y="120" class="subtitle">[nəʊd] n. 节点，结点</text>

  <!-- Etymology -->
  <text x="50" y="170" class="content">
    <tspan class="highlight">词根:</tspan> nod (点)
  </text>

  <!-- Story -->
  <text x="50" y="210" class="story">
    想象一个网络中的"点(nod)"，
    <tspan x="50" dy="25">这个点连接着其他点，形成了一个复杂的网络结构。</tspan>
  </text>

  <!-- Example -->
  <text x="50" y="280" class="content">
    <tspan class="highlight">例句:</tspan> The server acts as a node in the network.
  </text>
  <text x="50" y="310" class="content">
    服务器在网络中充当一个节点。
  </text>

  <!-- Visual representation -->
  <g transform="translate(400,320)">
    <!-- Node -->
    <circle cx="50" cy="50" r="20" class="image" />
    <circle cx="150" cy="50" r="20" class="image" />
    <circle cx="250" cy="50" r="20" class="image" />
    <!-- Connections -->
    <line x1="70" y1="50" x2="130" y2="50" class="image" />
    <line x1="170" y1="50" x2="230" y2="50" class="image" />
    <line x1="50" y1="70" x2="150" y2="70" class="image" />
    <line x1="150" y1="70" x2="250" y2="70" class="image" />
  </g>
</svg>