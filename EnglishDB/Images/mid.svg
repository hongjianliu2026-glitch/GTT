<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 800 600">
  <defs>
    <style>
      .background { fill: #E8F6F3; }
      .title { font: bold 48px sans-serif; fill: #16A085; }
      .subtitle { font: italic 24px serif; fill: #2C3E50; }
      .content { font: 18px sans-serif; fill: #34495E; }
      .highlight { font-weight: bold; fill: #E74C3C; }
      .story { font: 16px serif; fill: #2980B9; }
      .image { stroke: #7F8C8D; stroke-width: 2; fill: none; }
    </style>
  </defs>

  <!-- Background -->
  <rect class="background" width="100%" height="100%" />

  <!-- Title -->
  <text x="50" y="80" class="title">mid</text>
  <text x="50" y="120" class="subtitle">[mɪd] adj. 中间的，中部的</text>

  <!-- Etymology -->
  <text x="50" y="170" class="content">
    <tspan class="highlight">词根:</tspan> mid- (中间)
  </text>

  <!-- Story -->
  <text x="50" y="210" class="story">
    想象一条直线，"中间(mid)"有一个标记，
    <tspan x="50" dy="25">这个标记就是mid的象征。</tspan>
  </text>

  <!-- Example -->
  <text x="50" y="280" class="content">
    <tspan class="highlight">例句:</tspan> The mid-point of the journey was the most challenging.
  </text>
  <text x="50" y="310" class="content">
    旅程的中点是最具挑战性的。
  </text>

  <!-- Visual representation -->
  <g transform="translate(400,200)">
    <!-- Line -->
    <line x1="0" y1="100" x2="350" y2="100" class="image" />
    <!-- Mid point -->
    <circle cx="175" cy="100" r="10" class="image" />
    <!-- Text annotation -->
    <text x="165" y="130" class="content">mid</text>
  </g>
</svg>