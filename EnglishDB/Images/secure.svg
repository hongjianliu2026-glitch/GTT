<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 800 600">
  <defs>
    <style>
      .background { fill: #E8F6F3; }
      .title { font: bold 48px sans-serif; fill: #16A085; }
      .subtitle { font: italic 24px serif; fill: #2C3E50; }
      .content { font: 18px sans-serif; fill: #34495E; }
      .highlight { font-weight: bold; fill: #E74C3C; }
      .story { font: 16px serif; fill: #2980B9; }
      .image { stroke: #7F8C8D; stroke-width: 2; fill: none; }
    </style>
  </defs>

  <!-- Background -->
  <rect class="background" width="100%" height="100%" />

  <!-- Title -->
  <text x="50" y="80" class="title">secure</text>
  <text x="50" y="120" class="subtitle">[sɪˈkjʊr] adj. 安全的；v. 保护</text>

  <!-- Etymology -->
  <text x="50" y="170" class="content">
    <tspan class="highlight">词根:</tspan> se- (离开) + cur (关心)
  </text>

  <!-- Story -->
  <text x="50" y="210" class="story">
    想象一个保险箱，"离开(se-)"了所有的危险，
    <tspan x="50" dy="25">只有关心(cur)它的人才能打开，这就是安全(secure)。</tspan>
  </text>

  <!-- Example -->
  <text x="50" y="280" class="content">
    <tspan class="highlight">例句:</tspan> He secured the door before leaving the house.
  </text>
  <text x="50" y="310" class="content">
    他在离开房子前锁好了门。
  </text>

  <!-- Visual representation -->
  <g transform="translate(400,320)">
    <!-- Safe -->
    <rect x="0" y="0" width="200" height="200" class="image" />
    <!-- Lock -->
    <circle cx="100" cy="100" r="20" class="image" />
    <line x1="100" y1="120" x2="100" y2="180" class="image" />
    <line x1="80" y1="150" x2="120" y2="150" class="image" />
  </g>
</svg>