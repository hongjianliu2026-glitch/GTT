<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 800 600">
  <defs>
    <style>
      .background { fill: #E8F6F3; }
      .title { font: bold 48px sans-serif; fill: #16A085; }
      .subtitle { font: italic 24px serif; fill: #2C3E50; }
      .content { font: 18px sans-serif; fill: #34495E; }
      .highlight { font-weight: bold; fill: #E74C3C; }
      .story { font: 16px serif; fill: #2980B9; }
      .image { stroke: #7F8C8D; stroke-width: 2; fill: none; }
    </style>
  </defs>

  <!-- Background -->
  <rect class="background" width="100%" height="100%" />

  <!-- Title -->
  <text x="50" y="80" class="title">warehouse</text>
  <text x="50" y="120" class="subtitle">[ˈwerˌhaʊs] n. 仓库，货栈</text>

  <!-- Etymology -->
  <text x="50" y="170" class="content">
    <tspan class="highlight">词根:</tspan> ware (货物) + house (房子)
  </text>

  <!-- Story -->
  <text x="50" y="210" class="story">
    想象一个巨大的房子里堆满了各种货物，"货物房子(ware-house)"
    <tspan x="50" dy="25">就是仓库(warehouse)，专门用来存放货物的地方。</tspan>
  </text>

  <!-- Example -->
  <text x="50" y="280" class="content">
    <tspan class="highlight">例句:</tspan> The goods were stored in the warehouse until they were shipped.
  </text>
  <text x="50" y="310" class="content">
    这些货物被存放在仓库里，直到被运走。
  </text>

  <!-- Visual representation -->
  <g transform="translate(400,320)">
    <!-- Warehouse building -->
    <rect x="0" y="0" width="350" height="200" class="image" />
    <!-- Door -->
    <rect x="150" y="120" width="50" height="80" class="image" />
    <!-- Goods -->
    <rect x="50" y="20" width="100" height="80" class="image" />
    <rect x="200" y="20" width="100" height="80" class="image" />
    <rect x="50" y="120" width="100" height="60" class="image" />
    <rect x="200" y="120" width="100" height="60" class="image" />
  </g>
</svg>