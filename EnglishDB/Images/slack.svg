<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 800 600">
  <defs>
    <style>
      .background { fill: #E8F6F3; }
      .title { font: bold 48px sans-serif; fill: #16A085; }
      .subtitle { font: italic 24px serif; fill: #2C3E50; }
      .content { font: 18px sans-serif; fill: #34495E; }
      .highlight { font-weight: bold; fill: #E74C3C; }
      .story { font: 16px serif; fill: #2980B9; }
      .image { stroke: #7F8C8D; stroke-width: 2; fill: none; }
    </style>
  </defs>

  <!-- Background -->
  <rect class="background" width="100%" height="100%" />

  <!-- Title -->
  <text x="50" y="80" class="title">slack</text>
  <text x="50" y="120" class="subtitle">[slæk] adj. 松弛的，懈怠的；n. 松弛，懈怠</text>

  <!-- Etymology -->
  <text x="50" y="170" class="content">
    <tspan class="highlight">词根:</tspan> sl- (滑，松) + -ack (表示状态)
  </text>

  <!-- Story -->
  <text x="50" y="210" class="story">
    想象一根绳子，"滑(sl-)"下来，变得"松(ack)"了，
    <tspan x="50" dy="25">这就是松弛(slack)的状态。</tspan>
  </text>

  <!-- Example -->
  <text x="50" y="280" class="content">
    <tspan class="highlight">例句:</tspan> The rope went slack after the knot came undone.
  </text>
  <text x="50" y="310" class="content">
    绳结松开后，绳子变得松弛了。
  </text>

  <!-- Visual representation -->
  <g transform="translate(400,320)">
    <!-- Rope -->
    <line x1="50" y1="20" x2="50" y2="180" class="image" />
    <line x1="50" y1="180" x2="250" y2="180" class="image" />
    <!-- Knot -->
    <circle cx="50" cy="100" r="15" class="image" />
    <!-- Slack part -->
    <line x1="50" y1="180" x2="150" y2="220" class="image" />
    <line x1="150" y1="220" x2="250" y2="180" class="image" />
  </g>
</svg>