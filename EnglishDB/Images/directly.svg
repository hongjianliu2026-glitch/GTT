<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 800 600">
  <defs>
    <style>
      .background { fill: #E8F6F3; }
      .title { font: bold 48px sans-serif; fill: #16A085; }
      .subtitle { font: italic 24px serif; fill: #2C3E50; }
      .content { font: 18px sans-serif; fill: #34495E; }
      .highlight { font-weight: bold; fill: #E74C3C; }
      .story { font: 16px serif; fill: #2980B9; }
      .image { stroke: #7F8C8D; stroke-width: 2; fill: none; }
    </style>
  </defs>

  <!-- Background -->
  <rect class="background" width="100%" height="100%" />

  <!-- Title -->
  <text x="50" y="80" class="title">directly</text>
  <text x="50" y="120" class="subtitle">[dəˈrektli] adv. 直接地，立即</text>

  <!-- Etymology -->
  <text x="50" y="170" class="content">
    <tspan class="highlight">词根:</tspan> di- (分开) + rect (直)
  </text>

  <!-- Story -->
  <text x="50" y="210" class="story">
    想象一条笔直的道路，"分开(di-)"
    <tspan x="50" dy="25">所有障碍，直接通向目标，这就是"直接地(directly)"。</tspan>
  </text>

  <!-- Example -->
  <text x="50" y="280" class="content">
    <tspan class="highlight">例句:</tspan> She answered the question directly without hesitation.
  </text>
  <text x="50" y="310" class="content">
    她毫不犹豫地直接回答了问题。
  </text>

  <!-- Visual representation -->
  <g transform="translate(400,320)">
    <!-- Straight road -->
    <line x1="0" y1="0" x2="350" y2="0" class="image" />
    <!-- Target -->
    <circle cx="350" cy="0" r="20" class="image" />
    <!-- Obstacles -->
    <rect x="100" y="-10" width="20" height="20" class="image" />
    <rect x="200" y="-10" width="20" height="20" class="image" />
  </g>
</svg>