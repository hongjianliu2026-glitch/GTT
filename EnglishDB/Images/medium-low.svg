<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 800 600">
  <defs>
    <style>
      .background { fill: #E8F6F3; }
      .title { font: bold 48px sans-serif; fill: #16A085; }
      .subtitle { font: italic 24px serif; fill: #2C3E50; }
      .content { font: 18px sans-serif; fill: #34495E; }
      .highlight { font-weight: bold; fill: #E74C3C; }
      .story { font: 16px serif; fill: #2980B9; }
      .image { stroke: #7F8C8D; stroke-width: 2; fill: none; }
    </style>
  </defs>

  <!-- Background -->
  <rect class="background" width="100%" height="100%" />

  <!-- Title -->
  <text x="50" y="80" class="title">medium-low</text>
  <text x="50" y="120" class="subtitle">[ˈmiːdiəm loʊ] adj. 中低的</text>

  <!-- Etymology -->
  <text x="50" y="170" class="content">
    <tspan class="highlight">词根:</tspan> medium (中间) + low (低)
  </text>

  <!-- Story -->
  <text x="50" y="210" class="story">
    想象一个音量调节器，"中间(medium)"
    <tspan x="50" dy="25">和"低(low)"的结合，形成了一个中低音量(medium-low)。</tspan>
  </text>

  <!-- Example -->
  <text x="50" y="280" class="content">
    <tspan class="highlight">例句:</tspan> The speaker was set to a medium-low volume.
  </text>
  <text x="50" y="310" class="content">
    扬声器的音量设置为中低。
  </text>

  <!-- Visual representation -->
  <g transform="translate(400,320)">
    <!-- Volume control -->
    <rect x="0" y="0" width="350" height="200" class="image" />
    <!-- Volume bar -->
    <rect x="50" y="20" width="250" height="160" class="image" />
    <!-- Low volume indicator -->
    <line x1="70" y1="140" x2="280" y2="140" class="image" />
    <!-- Medium-low volume indicator -->
    <line x1="70" y1="80" x2="250" y2="80" class="image" />
    <!-- High volume indicator -->
    <line x1="70" y1="20" x2="270" y2="20" class="image" />
    <!-- Volume knob -->
    <circle cx="150" cy="80" r="15" class="image" />
  </g>
</svg>