<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 800 600">
  <defs>
    <style>
      .background { fill: #F9EBEA; }
      .title { font: bold 48px sans-serif; fill: #C0392B; }
      .subtitle { font: italic 24px serif; fill: #7B241C; }
      .content { font: 18px sans-serif; fill: #512E5F; }
      .highlight { font-weight: bold; fill: #E74C3C; }
      .story { font: 16px serif; fill: #2980B9; }
      .image { stroke: #7F8C8D; stroke-width: 2; fill: none; }
    </style>
  </defs>

  <!-- Background -->
  <rect class="background" width="100%" height="100%" />

  <!-- Title -->
  <text x="50" y="80" class="title">stain</text>
  <text x="50" y="120" class="subtitle">[steɪn] n. 污点，污渍；v. 弄脏，染色</text>

  <!-- Etymology -->
  <text x="50" y="170" class="content">
    <tspan class="highlight">词根:</tspan> sta- (站立) + in (里面)
  </text>

  <!-- Story -->
  <text x="50" y="210" class="story">
    想象一块白布上，"站立(sta-)"着"里面(in)"的污渍，
    <tspan x="50" dy="25">这个污渍就像一个顽固的污点(stain)，难以去除。</tspan>
  </text>

  <!-- Example -->
  <text x="50" y="280" class="content">
    <tspan class="highlight">例句:</tspan> The coffee stain on his shirt was hard to remove.
  </text>
  <text x="50" y="310" class="content">
    他衬衫上的咖啡渍很难去除。
  </text>

  <!-- Visual representation -->
  <g transform="translate(400,320)">
    <!-- White cloth -->
    <rect x="0" y="0" width="350" height="200" class="image" />
    <!-- Stain -->
    <circle cx="100" cy="100" r="30" fill="#8B4513" />
    <!-- Text label -->
    <text x="100" y="150" class="content" text-anchor="middle">污渍</text>
  </g>
</svg>