<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 800 600">
  <defs>
    <style>
      .background { fill: #E8F6F3; }
      .title { font: bold 48px sans-serif; fill: #16A085; }
      .subtitle { font: italic 24px serif; fill: #2C3E50; }
      .content { font: 18px sans-serif; fill: #34495E; }
      .highlight { font-weight: bold; fill: #E74C3C; }
      .story { font: 16px serif; fill: #2980B9; }
      .image { stroke: #7F8C8D; stroke-width: 2; fill: none; }
    </style>
  </defs>

  <!-- Background -->
  <rect class="background" width="100%" height="100%" />

  <!-- Title -->
  <text x="50" y="80" class="title">folder</text>
  <text x="50" y="120" class="subtitle">[ˈfoʊldər] n. 文件夹，折叠器</text>

  <!-- Etymology -->
  <text x="50" y="170" class="content">
    <tspan class="highlight">词根:</tspan> fold (折叠) + -er (表示物)
  </text>

  <!-- Story -->
  <text x="50" y="210" class="story">
    想象一个巨大的文件夹，"折叠(fold)"
    <tspan x="50" dy="25">着无数的纸张，这些纸张都被整齐地收纳在文件夹(folder)里。</tspan>
  </text>

  <!-- Example -->
  <text x="50" y="280" class="content">
    <tspan class="highlight">例句:</tspan> She organized her documents into different folders.
  </text>
  <text x="50" y="310" class="content">
    她把文件整理到不同的文件夹里。
  </text>

  <!-- Visual representation -->
  <g transform="translate(400,320)">
    <!-- Folder -->
    <rect x="0" y="0" width="350" height="200" class="image" />
    <rect x="50" y="20" width="250" height="160" class="image" />
    <!-- Fold lines -->
    <line x1="50" y1="20" x2="50" y2="180" class="image" />
    <line x1="50" y1="20" x2="300" y2="20" class="image" />
    <line x1="50" y1="180" x2="300" y2="180" class="image" />
    <!-- Papers -->
    <rect x="70" y="40" width="210" height="20" class="image" />
    <rect x="70" y="70" width="210" height="20" class="image" />
    <rect x="70" y="100" width="210" height="20" class="image" />
    <rect x="70" y="130" width="210" height="20" class="image" />
  </g>
</svg>