<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 800 600">
  <defs>
    <style>
      .background { fill: #E8F6F3; }
      .title { font: bold 48px sans-serif; fill: #16A085; }
      .subtitle { font: italic 24px serif; fill: #2C3E50; }
      .content { font: 18px sans-serif; fill: #34495E; }
      .highlight { font-weight: bold; fill: #E74C3C; }
      .story { font: 16px serif; fill: #2980B9; }
      .image { stroke: #7F8C8D; stroke-width: 2; fill: none; }
    </style>
  </defs>

  <!-- Background -->
  <rect class="background" width="100%" height="100%" />

  <!-- Title -->
  <text x="50" y="80" class="title">word</text>
  <text x="50" y="120" class="subtitle">[wɜːrd] n. 单词，话语；v. 用言辞表达</text>

  <!-- Etymology -->
  <text x="50" y="170" class="content">
    <tspan class="highlight">词根:</tspan> wor (说) + d (表示动作完成)
  </text>

  <!-- Story -->
  <text x="50" y="210" class="story">
    想象一个人在说话，"说(wor)"
    <tspan x="50" dy="25">出一个完整的词(word)，就像吐出一个珍珠。</tspan>
  </text>

  <!-- Example -->
  <text x="50" y="280" class="content">
    <tspan class="highlight">例句:</tspan> She spoke a single word and left the room.
  </text>
  <text x="50" y="310" class="content">
    她只说了一个词就离开了房间。
  </text>

  <!-- Visual representation -->
  <g transform="translate(400,320)">
    <!-- Mouth -->
    <circle cx="50" cy="100" r="30" class="image" />
    <!-- Word bubble -->
    <path d="M 80 70 Q 150 50 220 70 Q 250 80 250 110 Q 220 130 150 110 Q 80 90 80 70 Z" class="image" />
    <!-- Word inside bubble -->
    <text x="150" y="100" class="content" text-anchor="middle">word</text>
  </g>
</svg>