<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 800 600">
  <defs>
    <style>
      .background { fill: #F9EBEA; }
      .title { font: bold 48px sans-serif; fill: #C0392B; }
      .subtitle { font: italic 24px serif; fill: #2C3E50; }
      .content { font: 18px sans-serif; fill: #34495E; }
      .highlight { font-weight: bold; fill: #E74C3C; }
      .story { font: 16px serif; fill: #2980B9; }
      .image { stroke: #7F8C8D; stroke-width: 2; fill: none; }
    </style>
  </defs>

  <!-- Background -->
  <rect class="background" width="100%" height="100%" />

  <!-- Title -->
  <text x="50" y="80" class="title">baseball</text>
  <text x="50" y="120" class="subtitle">[ˈbeɪsbɔːl] n. 棒球</text>

  <!-- Etymology -->
  <text x="50" y="170" class="content">
    <tspan class="highlight">词根:</tspan> base (基础) + ball (球)
  </text>

  <!-- Story -->
  <text x="50" y="210" class="story">
    想象一个巨大的"基础(base)"上，一个"球(ball)"
    <tspan x="50" dy="25">在空中飞舞，这就是棒球(baseball)的场景。</tspan>
  </text>

  <!-- Example -->
  <text x="50" y="280" class="content">
    <tspan class="highlight">例句:</tspan> He plays baseball every weekend.
  </text>
  <text x="50" y="310" class="content">
    他每个周末都打棒球。
  </text>

  <!-- Visual representation -->
  <g transform="translate(400,320)">
    <!-- Base -->
    <rect x="0" y="0" width="350" height="200" class="image" />
    <!-- Ball -->
    <circle cx="175" cy="100" r="30" class="image" />
    <!-- Bat -->
    <line x1="220" y1="100" x2="350" y2="100" class="image" />
    <!-- Glove -->
    <rect x="50" y="150" width="50" height="30" class="image" />
  </g>
</svg>