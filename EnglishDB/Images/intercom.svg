<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 800 600">
  <defs>
    <style>
      .background { fill: #E8F6F3; }
      .title { font: bold 48px sans-serif; fill: #16A085; }
      .subtitle { font: italic 24px serif; fill: #2C3E50; }
      .content { font: 18px sans-serif; fill: #34495E; }
      .highlight { font-weight: bold; fill: #E74C3C; }
      .story { font: 16px serif; fill: #2980B9; }
      .image { stroke: #7F8C8D; stroke-width: 2; fill: none; }
    </style>
  </defs>

  <!-- Background -->
  <rect class="background" width="100%" height="100%" />

  <!-- Title -->
  <text x="50" y="80" class="title">intercom</text>
  <text x="50" y="120" class="subtitle">[ˈɪntərkɒm] n. 内部通信系统，对讲机</text>

  <!-- Etymology -->
  <text x="50" y="170" class="content">
    <tspan class="highlight">词根:</tspan> inter- (在...之间) + com (共同)
  </text>

  <!-- Story -->
  <text x="50" y="210" class="story">
    想象在一个大楼里，"在...之间(inter-)"
    <tspan x="50" dy="25">的各个房间里，人们通过一个"共同(com)"的通信系统</tspan>
    <tspan x="50" dy="25">进行对话，这个系统就是对讲机(intercom)。</tspan>
  </text>

  <!-- Example -->
  <text x="50" y="280" class="content">
    <tspan class="highlight">例句:</tspan> She used the intercom to call the security guard.
  </text>
  <text x="50" y="310" class="content">
    她用对讲机呼叫了保安。
  </text>

  <!-- Visual representation -->
  <g transform="translate(400,320)">
    <!-- Building -->
    <rect x="0" y="0" width="350" height="200" class="image" />
    <!-- Rooms -->
    <rect x="50" y="20" width="50" height="160" class="image" />
    <rect x="120" y="20" width="50" height="160" class="image" />
    <rect x="190" y="20" width="50" height="160" class="image" />
    <rect x="260" y="20" width="50" height="160" class="image" />
    <!-- Intercom -->
    <rect x="150" y="100" width="50" height="20" class="image" />
    <line x1="150" y1="110" x2="200" y2="110" class="image" />
    <circle cx="175" cy="110" r="5" class="image" />
  </g>
</svg>