<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 800 600">
  <defs>
    <style>
      .background { fill: #E8F6F3; }
      .title { font: bold 48px sans-serif; fill: #16A085; }
      .subtitle { font: italic 24px serif; fill: #2C3E50; }
      .content { font: 18px sans-serif; fill: #34495E; }
      .highlight { font-weight: bold; fill: #E74C3C; }
      .story { font: 16px serif; fill: #2980B9; }
      .image { stroke: #7F8C8D; stroke-width: 2; fill: none; }
    </style>
  </defs>

  <!-- Background -->
  <rect class="background" width="100%" height="100%" />

  <!-- Title -->
  <text x="50" y="80" class="title">sheet</text>
  <text x="50" y="120" class="subtitle">[ʃiːt] n. 床单，纸张，薄片</text>

  <!-- Etymology -->
  <text x="50" y="170" class="content">
    <tspan class="highlight">词根:</tspan> shet (古英语，表示覆盖物)
  </text>

  <!-- Story -->
  <text x="50" y="210" class="story">
    想象一张巨大的床单，"覆盖(shet)"
    <tspan x="50" dy="25">整个床，就像一张薄薄的纸张(sheet)。</tspan>
  </text>

  <!-- Example -->
  <text x="50" y="280" class="content">
    <tspan class="highlight">例句:</tspan> She changed the sheet on the bed.
  </text>
  <text x="50" y="310" class="content">
    她换了床上的床单。
  </text>

  <!-- Visual representation -->
  <g transform="translate(400,320)">
    <!-- Bed -->
    <rect x="0" y="0" width="350" height="200" class="image" />
    <!-- Sheet -->
    <rect x="50" y="20" width="250" height="160" class="image" fill="#F5F5F5" />
    <!-- Pillow -->
    <rect x="100" y="180" width="100" height="40" class="image" fill="#F5F5F5" />
    <!-- Bed legs -->
    <line x1="50" y1="200" x2="50" y2="220" class="image" />
    <line x1="300" y1="200" x2="300" y2="220" class="image" />
  </g>
</svg>