<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 800 600">
  <defs>
    <style>
      .background { fill: #F9EBEA; }
      .title { font: bold 48px sans-serif; fill: #C0392B; }
      .subtitle { font: italic 24px serif; fill: #7B241C; }
      .content { font: 18px sans-serif; fill: #512E5F; }
      .highlight { font-weight: bold; fill: #E74C3C; }
      .story { font: 16px serif; fill: #8E44AD; }
      .image { stroke: #A93226; stroke-width: 2; fill: none; }
    </style>
  </defs>

  <!-- Background -->
  <rect class="background" width="100%" height="100%" />

  <!-- Title -->
  <text x="50" y="80" class="title">fleck</text>
  <text x="50" y="120" class="subtitle">[flek] n. 斑点，微粒</text>

  <!-- Etymology -->
  <text x="50" y="170" class="content">
    <tspan class="highlight">词根:</tspan> fleck (源自中古英语，意为“斑点”)
  </text>

  <!-- Story -->
  <text x="50" y="210" class="story">
    想象一片洁白的雪花上，"突然出现(fleck)"
    <tspan x="50" dy="25">一个小小的黑色斑点，这个斑点就是fleck。</tspan>
  </text>

  <!-- Example -->
  <text x="50" y="280" class="content">
    <tspan class="highlight">例句:</tspan> The snowflake had a tiny fleck of dirt.
  </text>
  <text x="50" y="310" class="content">
    雪花上有一个微小的污点。
  </text>

  <!-- Visual representation -->
  <g transform="translate(400,200)">
    <!-- Snowflake -->
    <circle cx="150" cy="150" r="100" class="image" />
    <!-- Fleck -->
    <circle cx="100" cy="120" r="10" class="image" fill="#000" />
  </g>
</svg>